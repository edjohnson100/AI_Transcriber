# Mac dependencies for generic_transcriber.py
# Primary path uses the whisper.cpp binary — no Python ML packages required.
# openai-whisper is only needed for the Python fallback (if whisper.cpp is unavailable).

openai-whisper
